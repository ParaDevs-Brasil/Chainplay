node_modules/
dist/
.env
.env.railway
server/.data/
.vercel/

# Segredos documentados do time (seeds da devnet) — nunca commitar
keys_contract.md
program/keys/

# Lockfiles npm/yarn fora do versionamento (decisão do time)
package-lock.json
yarn.lock
